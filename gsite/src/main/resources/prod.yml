server:
  port: 10001
spring:
  data:
    mongodb:
      uri: mongodb://localhost/gsite
      host: localhost
  mvc:
    view:
      prefix:
        /
      suffix:
        .html

endpoints:
  cors:
    allowed-headers: '*'
    allowed-origins: '*'
    allowed-methods: '*'